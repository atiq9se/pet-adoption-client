import { Helmet } from 'react-helmet-async';

const Register = () => {
    return (
        <div>
            <Helmet>
                <title>Register Pet-adoption</title>
            </Helmet>
        </div>
    );
};

export default Register;